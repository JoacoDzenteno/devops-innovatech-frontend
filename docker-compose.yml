version: '3.8'

services:
  frontend:
    build: .
    image: innovatech-front-despacho:latest
    container_name: frontend_despacho
    ports:
      - "4173:4173"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
